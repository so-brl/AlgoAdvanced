jQuery(document).ready(function () {
    $('.btn').bind('click touch', function () {
        let choice = $(this).attr('id');
        console.log(choice);
        if (typeof choice ==="string"){

        }
        if (typeof choice ==="string"){

        }



    } )
});